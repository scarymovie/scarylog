# scarylog

This project `scarylog` is a Go module designed to provide a convenient wrapper around Go's structured logger, `slog`. The main goal is to simplify logging operations and provide consistent logging practices across applications.

## Installation

To install the package, run:

```bash
  go get github.com/scarymovie/scarylog
```